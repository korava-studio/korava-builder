# Dependency Rules

1. Prefer runtime dependencies only for production features.
2. Keep devDependencies limited to build, lint, and test tooling.
3. Avoid adding transitive dependencies unless necessary.
4. Prefer standard Node APIs and small libraries over large frameworks.
5. Review dependency updates for compatibility before upgrading.
