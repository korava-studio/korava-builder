# Project Structure

The repository follows a consistent structure:
- `src/` contains core application code and plugin support.
- `templates/` contains project scaffolding templates.
- `plugins/` contains plugin definitions and manifests.
- `docs/` contains architecture, coding, and development guidance.
- `tests/` contains automated tests when added.

Keep feature directories shallow and avoid deeply nested folders unless necessary.
